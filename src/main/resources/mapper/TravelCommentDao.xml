<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lvcong.emprise.dao.TravelCommentDao">

    <resultMap type="com.lvcong.emprise.entity.TravelComment" id="TravelCommentMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="targetId" column="target_id" jdbcType="INTEGER"/>
        <result property="parentCommentId" column="parent_comment_id" jdbcType="INTEGER"/>
        <result property="parentCommentUserId" column="parent_comment_user_id" jdbcType="INTEGER"/>
        <result property="replyCommentId" column="reply_comment_id" jdbcType="INTEGER"/>
        <result property="replyCommentUserId" column="reply_comment_user_id" jdbcType="INTEGER"/>
        <result property="commentLevel" column="comment_level" jdbcType="INTEGER"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="praiseNum" column="praise_num" jdbcType="INTEGER"/>
        <result property="topStatus" column="top_status" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TravelCommentMap">
        select
          id, user_id, target_id, parent_comment_id, parent_comment_user_id, reply_comment_id, reply_comment_user_id, comment_level, content, status, praise_num, top_status, create_time
        from travel_comment
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TravelCommentMap">
        select
          id, user_id, target_id, parent_comment_id, parent_comment_user_id, reply_comment_id, reply_comment_user_id, comment_level, content, status, praise_num, top_status, create_time
        from travel_comment
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="targetId != null">
                and target_id = #{targetId}
            </if>
            <if test="parentCommentId != null">
                and parent_comment_id = #{parentCommentId}
            </if>
            <if test="parentCommentUserId != null">
                and parent_comment_user_id = #{parentCommentUserId}
            </if>
            <if test="replyCommentId != null">
                and reply_comment_id = #{replyCommentId}
            </if>
            <if test="replyCommentUserId != null">
                and reply_comment_user_id = #{replyCommentUserId}
            </if>
            <if test="commentLevel != null">
                and comment_level = #{commentLevel}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="praiseNum != null">
                and praise_num = #{praiseNum}
            </if>
            <if test="topStatus != null">
                and top_status = #{topStatus}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from travel_comment
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="targetId != null">
                and target_id = #{targetId}
            </if>
            <if test="parentCommentId != null">
                and parent_comment_id = #{parentCommentId}
            </if>
            <if test="parentCommentUserId != null">
                and parent_comment_user_id = #{parentCommentUserId}
            </if>
            <if test="replyCommentId != null">
                and reply_comment_id = #{replyCommentId}
            </if>
            <if test="replyCommentUserId != null">
                and reply_comment_user_id = #{replyCommentUserId}
            </if>
            <if test="commentLevel != null">
                and comment_level = #{commentLevel}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="praiseNum != null">
                and praise_num = #{praiseNum}
            </if>
            <if test="topStatus != null">
                and top_status = #{topStatus}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into travel_comment(user_id, target_id, parent_comment_id, parent_comment_user_id, reply_comment_id, reply_comment_user_id, comment_level, content, status, praise_num, top_status, create_time)
        values (#{userId}, #{targetId}, #{parentCommentId}, #{parentCommentUserId}, #{replyCommentId}, #{replyCommentUserId}, #{commentLevel}, #{content}, #{status}, #{praiseNum}, #{topStatus}, #{createTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into travel_comment(user_id, target_id, parent_comment_id, parent_comment_user_id, reply_comment_id, reply_comment_user_id, comment_level, content, status, praise_num, top_status, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.userId}, #{entity.targetId}, #{entity.parentCommentId}, #{entity.parentCommentUserId}, #{entity.replyCommentId}, #{entity.replyCommentUserId}, #{entity.commentLevel}, #{entity.content}, #{entity.status}, #{entity.praiseNum}, #{entity.topStatus}, #{entity.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into travel_comment(user_id, target_id, parent_comment_id, parent_comment_user_id, reply_comment_id, reply_comment_user_id, comment_level, content, status, praise_num, top_status, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.targetId}, #{entity.parentCommentId}, #{entity.parentCommentUserId}, #{entity.replyCommentId}, #{entity.replyCommentUserId}, #{entity.commentLevel}, #{entity.content}, #{entity.status}, #{entity.praiseNum}, #{entity.topStatus}, #{entity.createTime})
        </foreach>
        on duplicate key update
        user_id = values(user_id),
        target_id = values(target_id),
        parent_comment_id = values(parent_comment_id),
        parent_comment_user_id = values(parent_comment_user_id),
        reply_comment_id = values(reply_comment_id),
        reply_comment_user_id = values(reply_comment_user_id),
        comment_level = values(comment_level),
        content = values(content),
        status = values(status),
        praise_num = values(praise_num),
        top_status = values(top_status),
        create_time = values(create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update travel_comment
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="targetId != null">
                target_id = #{targetId},
            </if>
            <if test="parentCommentId != null">
                parent_comment_id = #{parentCommentId},
            </if>
            <if test="parentCommentUserId != null">
                parent_comment_user_id = #{parentCommentUserId},
            </if>
            <if test="replyCommentId != null">
                reply_comment_id = #{replyCommentId},
            </if>
            <if test="replyCommentUserId != null">
                reply_comment_user_id = #{replyCommentUserId},
            </if>
            <if test="commentLevel != null">
                comment_level = #{commentLevel},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="praiseNum != null">
                praise_num = #{praiseNum},
            </if>
            <if test="topStatus != null">
                top_status = #{topStatus},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from travel_comment where id = #{id}
    </delete>

</mapper>

