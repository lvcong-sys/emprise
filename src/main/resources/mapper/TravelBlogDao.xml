<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=".dao.TravelBlogDao">

    <resultMap type="com.lvcong.emprise.entity.TravelBlog" id="TravelBlogMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="context" column="context" jdbcType="VARCHAR"/>
        <result property="time" column="time" jdbcType="TIMESTAMP"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="authorId" column="author_id" jdbcType="INTEGER"/>
        <result property="location" column="location" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TravelBlogMap">
        select
          id, title, context, time, status, author_id, location
        from travel_blog
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TravelBlogMap">
        select
          id, title, context, time, status, author_id, location
        from travel_blog
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="context != null and context != ''">
                and context = #{context}
            </if>
            <if test="time != null">
                and time = #{time}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="authorId != null">
                and author_id = #{authorId}
            </if>
            <if test="location != null">
                and location = #{location}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from travel_blog
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="context != null and context != ''">
                and context = #{context}
            </if>
            <if test="time != null">
                and time = #{time}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="authorId != null">
                and author_id = #{authorId}
            </if>
            <if test="location != null">
                and location = #{location}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into travel_blog(title, context, time, status, author_id, location)
        values (#{title}, #{context}, #{time}, #{status}, #{authorId}, #{location})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into travel_blog(title, context, time, status, author_id, location)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.title}, #{entity.context}, #{entity.time}, #{entity.status}, #{entity.authorId}, #{entity.location})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into travel_blog(title, context, time, status, author_id, location)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.title}, #{entity.context}, #{entity.time}, #{entity.status}, #{entity.authorId}, #{entity.location})
        </foreach>
        on duplicate key update
        title = values(title),
        context = values(context),
        time = values(time),
        status = values(status),
        author_id = values(author_id),
        location = values(location)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update travel_blog
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="context != null and context != ''">
                context = #{context},
            </if>
            <if test="time != null">
                time = #{time},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="authorId != null">
                author_id = #{authorId},
            </if>
            <if test="location != null">
                location = #{location},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from travel_blog where id = #{id}
    </delete>

</mapper>

